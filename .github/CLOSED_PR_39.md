# Closed due to conflicts with duplicate commits. See PR #40.

This PR had merge conflicts because develop contained commit `0fdfea5` which was functionally identical to main's `7b8bfea`. The same changes have been applied cleanly in PR #40 from branch `fix-docs-version-1.1.0`.

Resolved by: PR #40 (merged as `646a2ce`).